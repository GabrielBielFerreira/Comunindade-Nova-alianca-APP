import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary — Vinho/Bordô institucional
  static const primary = Color(0xFF7A0022);
  static const primaryHover = Color(0xFF5C0019);
  static const primaryLight = Color(0xFF9B1335);
  static const primarySoft = Color(0xFFF5E6EC);
  static const primaryMuted = Color(0xFFEACDD6);
  static const onPrimary = Color(0xFFFFFFFF);

  // Accent — Vermelho vivo para CTAs de urgência
  static const accent = Color(0xFFC0392B);
  static const accentSoft = Color(0xFFFCEAE8);
  static const onAccent = Color(0xFFFFFFFF);

  // Surfaces — tema LIGHT apenas
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceMuted = Color(0xFFF3F4F6);

  // Text
  static const foreground = Color(0xFF1A1A1A);
  static const mutedForeground = Color(0xFF6B7280);
  static const muted = Color(0xFF9CA3AF);

  // Borders
  static const border = Color(0xFFE5E7EB);
  static const borderFocus = Color(0xFF7A0022);

  // Status
  static const success = Color(0xFF16A34A);
  static const successSoft = Color(0xFFDCFCE7);
  static const warning = Color(0xFFEAB308);
  static const warningSoft = Color(0xFFFEF9C3);
  static const error = Color(0xFFDC2626);
  static const errorSoft = Color(0xFFFEE2E2);
}
